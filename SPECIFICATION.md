# Connexy 仕様書(現行実装ベース)

## 0. このドキュメントについて

本ドキュメントは、理想像や要求仕様ではなく、**現時点のソースコード実装を読み解いて起こしたリバースエンジニアリング仕様書**です。2026-08-24 時点の `main` ブランチをもとに作成しています。

- 実データ(Supabase)で動く部分と、ハードコードされたモック/デモ用データで動いている部分が混在しています。本書ではその区別が分かるよう、モック/未実装の箇所には **⚠️モック** の注記を付けています。
- 既知のバグや今後の対応タスクは本書には含めていません。[todo.md](todo.md) を参照してください。
- 該当コードの場所が分かるよう、主要な仕様には `ファイル名:行番号` の形式で参照を付けています(行番号は今後のコード変更でずれる可能性があります)。

---

## 1. プロダクト概要

**Connexy** は、通信キャリアショップ(docomo/au・UQ mobile/SoftBank・Y!mobile/楽天モバイル等)や家電量販店(ヤマダ電機、ビックカメラ、ヨドバシカメラ等)の**販売スタッフの人材マッチングを行うBtoB Webアプリ**です。

- **発注企業(client)**: イベント・店舗販売の案件(`Job`)を掲載し、人材を募集する側。
- **受注企業(agency)**: 自社の登録スタッフ(`Staff`)を案件に応募・提案する側。
- 1社が両方の役割を兼ねる(`both`)ことも可能。

マッチング成立後は、Connexy上でチャット・内定・契約承認・完了報告・相互評価・請求書発行までを一気通貫で行い、Connexyは成約額に対して**マッチング手数料**を徴収するビジネスモデルです([§7.3](#73-手数料))。

### 主なドメイン用語

| 用語 | 意味 |
|---|---|
| 職種(roleType) | キャンペーンクルー / クローザー / ディレクター |
| キャリア(carrier) | docomo / au・UQmobile / SoftBank・Y!mobile / BB(固定回線) |
| 販路(salesChannel) | ショップ(キャリアショップ) / 量販店(家電量販店) |
| 勤務形態(workLocation) | 店内 / 外販(複合施設など) / 外販(スーパーなど) / 外販(その他) |
| 案件コード(jobCode) | `JOB-XXXXXX` 形式の一意な案件識別子 |

---

## 2. システム構成

- **フロントエンド**: React 19 + TypeScript + Vite([package.json](package.json))。ルーティングは `react-router-dom` v7 だが、v7のデータルーターAPIは使わず、旧来の `HashRouter` + `<Routes>/<Route>` 宣言的APIを使用([src/App.tsx:1,57-84](src/App.tsx))。
- **地図**: Google Maps JavaScript API(`@googlemaps/js-api-loader` + Advanced Markers + `@googlemaps/markerclusterer`)。ジオコーディングのみ Google ではなく **Nominatim(OpenStreetMap)** の無料APIを利用(プロバイダ混在)。
- **PDF/カレンダー出力**: `jsPDF`(請求書PDF)、独自実装の `.ics` 出力(カレンダー同期)。
- **バックエンド**: Supabase(PostgreSQL + Auth基盤)を利用する想定だが、**オフライン/失敗時は自動的に `localStorage` ベースのモックDBにフォールバック**するハイブリッド構成([§2.1](#21-データ層のハイブリッド構成))。

### 2.1 データ層のハイブリッド構成

すべての `api.*` 呼び出しは共通ラッパー `callSupabase(apiFn, fallbackFn)`([src/data/mockDb.ts:570-584](src/data/mockDb.ts))を経由します。

1. 起動時に `localStorage['connexy_is_offline']` または `navigator.onLine` を見て、オフラインモードかどうかを判定。
2. オンライン時はまず Supabase 呼び出しを試み、**例外が発生した時点で自動的にオフラインモードへ切り替わり**、以後はリロードしても `localStorage` ベースの擬似DB(`offline_db_<table>` キー)で動作し続けます。
3 . 会社プロフィールの一部項目・スタッフの権限/ログイン情報・お気に入り・契約書テンプレート等は、Supabase が生きている場合でも**個別に `localStorage` へキャッシュ/上書き保存**されており、"真実のデータ"がブラウザのローカルストレージにしか存在しないケースがあります([src/data/mockDb.ts](src/data/mockDb.ts) `mapUser` 等)。

この設計により、デモ環境や通信不良時でもアプリが動作し続けますが、**複数端末・複数ブラウザ間でのデータ整合性は保証されません**。

---

## 3. ユーザー・権限モデル

Connexyのアカウントは「会社」を主体とし、その配下に「スタッフ(個人)」がぶら下がる構造です。権限は独立した3つの軸で決まります。

### 3.1 軸A: ログイン主体(会社本体 or 配下スタッフ)

| ログイン種別 | 判定条件 | できること |
|---|---|---|
| 会社管理者(本体アカウント) | `staffId` が無い | 会社の全操作(案件掲載・スタッフ管理・契約承認・財務確認等) |
| 管理者権限スタッフ | `staffId` あり かつ `staffRole==='admin'` | 会社管理者とほぼ同等に扱われる(`isUserAdmin = !currentUser.staffId \|\| currentUser.staffRole==='admin'` が全体で使われる共通判定式) |
| 一般スタッフ | `staffId` あり かつ `staffRole==='staff'` | 自分がアサインされた現場・自分宛のチャット・自分の出退勤/報告のみ閲覧可能。会社全体のデータは見えない |

### 3.2 軸B: 会社の事業種別(companyType)

| 値 | 意味 |
|---|---|
| `client` | 発注企業(案件を掲載する側) |
| `agency` | 受注企業(スタッフを提案・派遣する側) |
| `both` | 両方の役割を兼ねる |

この区分は主に画面ごとの表示条件分岐に使われる**アプリケーションレベルの制御**であり、DBスキーマ上の強制ではありません(例: [SearchPage.tsx:2857](src/pages/SearchPage.tsx) で `companyType==='agency'||'both'` またはジョブの投稿者本人のみ応募操作を表示)。

### 3.3 軸C: アカウント承認ステータス(status)

| 値 | 意味 |
|---|---|
| `pending` | 新規登録直後。ログイン不可(審査中メッセージを表示しログアウトさせる) |
| `approved` | 通常利用可能 |
| `rejected` | 登録却下。ログイン不可 |

新規会社登録は必ず `pending` から始まり、管理者による承認が必要です([src/data/mockDb.ts:1608](src/data/mockDb.ts))。

---

## 4. 認証・アカウント登録

### 4.1 ログイン([src/pages/LoginPage.tsx](src/pages/LoginPage.tsx))

- `loginId` + `password` の単純な照合(ハッシュ化なし)。会社アカウントとスタッフアカウントは同じログインフォームを共有します。
- ログイン成功後、`status` を確認し `pending`/`rejected` の場合は強制ログアウトしてアラート表示。
- ロールに関する選択UIは無く、ログインした主体(会社 or スタッフ行)によって自動的に決まります。

### 4.2 新規会社登録

必須項目: 法人名、担当者名、ログインID、パスワード。任意: 代表者名、インボイス登録番号(`^T\d{13}$` 形式でバリデーション)。登記簿謄本コピーのアップロード欄がありますが、**⚠️モック**: 選択したファイルは実際には送信・保存されません(UIスタブのみ)。登録すると `status: 'pending'` で作成されます。

### 4.3 テストアカウント機能・デバッグパネル

- 「テストアカウントで簡単ログイン」: sigma/alpha/beta/gamma/delta/seals/freer/cocolabo の8社デモ企業と、その配下スタッフをワンタップで選んでログインできる機能。
- ⚠️**要注意**: ログイン画面に「デバッグ開発用アカウント審査管理ツール」というパネルが常設されており、**未ログイン状態から誰でも**全社・全スタッフのログインID/パスワードを平文で閲覧し、会社の承認ステータスを変更できます([src/pages/LoginPage.tsx:409-497](src/pages/LoginPage.tsx))。本番公開前に必ず削除・アクセス制限が必要な箇所です。

### 4.4 セッション管理

Cookie/JWTは使用せず、`localStorage` のキーで管理:
- `connexy_current_user_id`: ログイン中の会社ID
- `connexy_current_staff_id`: スタッフとしてログインしている場合のみセット
- その他、会社/スタッフのプロフィール項目ごとのキャッシュキーが多数存在

### 4.5 ルーティングガード([src/App.tsx](src/App.tsx))

- 未ログイン時は `/login` のみアクセス可能、それ以外は `/login` にリダイレクト。
- ログイン済みなら `/`(ホーム)、`/search`(探す)、`/message`(メッセージ)、`/manage`(管理)のみが有効。`/login` へのアクセスは `/` にリダイレクト。
- **`pending` ステータス専用のルートガードは存在しません**(ログイン時点で弾かれるため、ルーター側では未考慮)。

---

## 5. データモデル

以下は [src/data/mockDb.ts](src/data/mockDb.ts) 上のTypeScript型定義に基づきます。実際のSupabaseスキーマとはキャメルケース/スネークケースの相互変換(`mapX`/`unmapX`)を介して対応しています。

### 5.1 User(会社アカウント)

| フィールド | 型/値 | 備考 |
|---|---|---|
| id | string | |
| name | string | 法人名 |
| role | `contractor`\|`worker`\|`admin` | レガシー的な汎用ロール。実運用は `companyType` が主 |
| loginId / password | string | 平文 |
| status | `pending`\|`approved`\|`rejected` | [§3.3](#33-軸c-アカウント承認ステータスstatus) |
| invoiceNumber | string | インボイス制度登録番号 |
| staffId / staffName / staffRole | - | **DB保存項目ではなく**、ログイン時に動的に付与される「今誰が操作しているか」の情報 |
| representativeName | string | 代表者名 |
| companyType | `client`\|`agency`\|`both` | [§3.2](#32-軸b-会社の事業種別companytype) |
| favoriteJobIds / favoriteTalentIds / favoriteFolders | array | お気に入り管理 |

### 5.2 Staff(スタッフ個人)

| フィールド | 備考 |
|---|---|
| id, userId(所属会社), name, maskedName | `maskedName` はマッチング前の一覧表示用の匿名名 |
| role | `admin`\|`staff`([§3.1](#31-軸a-ログイン主体会社本体-or-配下スタッフ)) |
| price, skills[], carriers[], experience, prText | プロフィール情報 |
| loginId / password | スタッフ個別ログイン用 |
| gender, birthday, furigana, commuteMethod | 追加属性 |
| completedTrainings[] | ⚠️**設計上の注意**: 研修修了IDだけでなく、`ATTENDANCE_LOG_<日付>_<時刻>_<OK\|LATE>`(出退勤ログ)、`STAFF_FURIGANA_*` 等のタグ文字列も同じ配列に混在させて保存されている(専用フィールドが無いための代用実装) |

### 5.3 Job(案件)

| フィールド | 備考 |
|---|---|
| id, title, description, authorId | authorId=掲載企業(client) |
| lat/lng | **あいまい化(市区町村中心などに丸めた)座標**。公開マップに使用 |
| exactLat/exactLng/exactLocation | 正確な座標・住所。マッチング前は非公開 |
| price / dailyPrices | 固定日給、または日付ごとの単価マップ |
| roleType, carrier, salesChannel, workLocation | [§1のドメイン用語](#主なドメイン用語) |
| eventDate, applicationDeadline, isUrgent | 開催日・応募締切・緊急募集フラグ |
| allowedCompanyIds | 特定企業限定の非公開案件機能 |
| expenses | 交通費/宿泊費の支給方式(なし/別途支給/手配/定額/実費) |
| jobCode | `JOB-XXXXXX` 形式で採番 |
| status | `active`\|`cancelled` |

⚠️`dailyPrices` と `expenses` は、Supabase側に専用カラムが無いため `requirements[]` 配列内にシリアライズ文字列(`__DAILY_PRICES__::...` 等)として埋め込んで保存する回避策が取られています。

### 5.4 Talent(人材公開プロフィール)

Staffとは別に、マーケットプレイスで検索・閲覧される「公開用の人材カード」として存在します。`maskedName` のみが表示され、実名 `name` はUIには出しませんが、クライアント側のデータオブジェクトには含まれているため、ネットワークレベルでは実名が転送されています(実バックエンド接続時のプライバシー上の留意点)。

### 5.5 Training(研修)

固定マスタデータ(3件): 接客基礎マナー研修、クロージング・光回線営業スキル研修、ディレクター・現場責任者研修。研修は動画+理解度クイズ形式(合格ライン80%)。

### 5.6 Evaluation(評価)

`rating`(1〜5)、`comment`、`evaluatorName`、`createdAt`、`hasLateness`(遅刻等トラブル有無)。`ContractTask.evaluations` 内に `byClient` / `byWorker` / `byStaffToAgency` / `byAgencyToStaff` / `byStaffToField` の役割別マップとして保持され、双方向の相互評価を実現しています。

### 5.7 ContractTask(取引・チャットの中核エンティティ)

**この型は「契約・取引」と「チャットスレッド」の両方を兼ねる**、システムの中心的なエンティティです。

| フィールド | 備考 |
|---|---|
| id | 直接チャットは `chat_<会社A>_<会社B>`(ID昇順ソート)、グループチャットは `chat_group_<jobId>_<会社ID>` 形式 |
| status | 下記[§7.1のライフサイクル](#71-取引ライフサイクル状態遷移) |
| evaluations | メッセージ履歴(`messages[]`)、応募案件ID一覧(`appliedJobIds[]`)、アサインスタッフ(`appliedJobStaffIds`)、内定情報(`offeredJobId` 等)もすべてこのJSONカラムに格納 |
| disputedReason | 異議申し立て時の理由 |
| clientContractApprovedByClient/Agency, agencyContractApprovedByClient/Agency | 契約書(発注元/受注元それぞれが提示する契約文面)への双方合意フラグ |

---

## 6. 画面仕様

### 6.1 ホーム(`/`, [HomePage.tsx](src/pages/HomePage.tsx))

| セクション | 内容 |
|---|---|
| 運営からのお知らせ | ⚠️モック: `mockAnnouncements` というハードコード配列3件を表示するのみ。バックエンド連携なし |
| 売上・入出金状況 | 「入金予定額」「振込予定額」のタップで内訳画面へ。⚠️**内訳の金額・案件明細・手数料額はすべて画面内に直書きされた固定値**であり、実データからの計算ではありません |
| 本日の現場・出退勤打刻 | GPS打刻(実際のGPS測位ではなく「GPSシミュレーター」チェックボックスでON/OFFする簡易実装)。⚠️表示されている現場名・時間・案件コードも固定値 |
| タスクサマリー | 報告待ち件数・内定回答待ち件数は `tasks` から実計算 |

### 6.2 探す(`/search`, [SearchPage.tsx](src/pages/SearchPage.tsx))

**案件モード**と**人材モード**を切り替える、マップ/リスト表示両対応の検索画面です。

#### 共通
- キーワード検索、お気に入りのみ表示、エリア絞り込み(⚠️新宿・渋谷・池袋の3エリアのみハードコード、実際の半径検索ではない)
- 保存済みフィルタ・名前付き検索プリセットの保存/呼び出し

#### 案件モード
- 並び替え(新着順/単価高い順/開催日近い順)、職種・キャリア・販路・最低日給・応募締切日数での絞り込み、緊急募集の表示切替
- 案件カード: 締切バッジ(当日/3日以内/1週間以内で色分け)、取引実績ありバッジ、価格表示
- 応募フロー: 自社スタッフを1名選択 → 「同意して応募」チェック必須のNDA同意モーダル → チャットルーム作成 → `/message` へ遷移。応募済み・進行中/完了の案件は以後の検索結果から自動除外

#### 人材モード
- 人材カード: `maskedName` のみ表示、認証済みバッジ、対応キャリア/スキル、受講済み研修、価格、稼働可能日
- 複数人材を選択して一括スカウトメッセージ送信が可能
- 個別に「メッセージを送る」でチャット開始(自社人材への送信はブロックされる)

#### 地図機能
- Google Maps上に案件/人材をエリアごとにクラスタリング表示(座標は市区町村レベルに丸めてプライバシー保護)
- 住所検索・現在地取得・地図クリックでの位置指定(逆ジオコーディングはNominatim経由)

#### プライバシー保護ロジック([src/utils/maskingUtils.ts](src/utils/maskingUtils.ts))
- `extractArea`: 住所から市区町村・エリア名のみを抽出
- `generateMaskedLocation`: 店舗の正式名称・住所から、キャリア/量販店チェーン名を検知して「新宿の量販店」等の抽象化された表示名を自動生成。案件の正確な店舗名は成約前は非公開

### 6.3 メッセージ(`/message`, [MessagePage.tsx](src/pages/MessagePage.tsx))

- **チャンネル種別**: ①固定グループチャット(`chat_au_group`)、②案件・企業単位の動的グループチャット(`chat_group_<jobId>_<会社ID>`)、③企業間の直接1:1チャット(`chat_<会社A>_<会社B>`)
- **アクセス制御**: 一般スタッフは自分がアサインされたグループチャットのみ閲覧可能。管理者権限スタッフは会社の代表として直接チャット・全グループチャットを閲覧可能([§3.1](#31-軸a-ログイン主体会社本体-or-配下スタッフ)参照。当初は管理者スタッフも一般スタッフと同様に制限されていたが、後に修正)
- **連絡先マスキング**(`maskContactInfo`): 契約成立(`contracted`)・グループチャット以外では、メールアドレス・電話番号・LINE IDらしき文字列をメッセージ表示時に自動的に `[連絡先はマッチング完了まで非公開です]` に置換し、マッチング手数料を回避した直接連絡先交換を抑止
- **内定〜契約フロー**: 内定通知のチャットバブル表示 → 承諾/辞退の二重確認モーダル → 承諾で正式契約(`working`)状態へ、他の競合応募は自動的に不採用(`rejected`)化
- **経費申請**: 交通費・宿泊費等の申請機能(すべての案件で申請可能)
- **参加メンバー一覧**モーダル(グループチャット)

### 6.4 管理(`/manage`, [ManagementPage.tsx](src/pages/ManagementPage.tsx))

管理者向け/一般スタッフ向けでメニュー構成が異なります。

| メニュー | 管理者 | 一般スタッフ | 概要 |
|---|:-:|:-:|---|
| 案件管理 | ○ | - | 自社掲載案件の一覧・キャンセル、候補者選考・内定オファー。⚠️**新規作成・編集・複製フォームはこの画面には存在しない**(別画面/別フロー想定) |
| 応募状況・履歴 | ○ | ○ | 他社案件への応募進捗・内定・履歴の一覧表示、チャットへのショートカット |
| スタッフ管理 | ○ | - | スタッフの追加・編集・権限(管理者/一般)の一括変更。ログインID/パスワードは⚠️**画面上に平文表示** |
| 報告・評価 | ○ | ○ | 完了報告の提出、双方向評価(星評価+コメント)、請求書PDF/ICSカレンダー出力、CSV一括出力 |
| 出勤管理 | ○ | ○ | 出勤予定カレンダー、NG日(稼働不可日)設定、出勤履歴とCSV出力 |
| 分析・ダッシュボード | ○ | - | ⚠️**極めて簡易的**: 「総案件・対応数」「完了実績率」の2指標のみ。グラフ等の実装なし |
| 研修・クイズ | - | ○ | ⚠️「準備中」のロック画面のみで実コンテンツなし |

**選考ロジック**: 候補企業の信頼性スコアは `出勤率×0.6 + (平均評価/5)×400` という式で算出されますが、⚠️候補企業データ自体が4社の固定モックであり、実際の登録企業から検索されるものではありません。距離も「新宿」「渋谷」等の文字列一致による簡易推定です。

### 6.5 設定(`SettingsDrawer`, 全ページ共通)

ホーム画面右上のハンバーガーメニューから開くスライドインパネル。会社プロフィール編集(法人名、インボイス番号、連絡先、契約書PDFアップロード等、管理者のみ)、決済連携(⚠️Stripe連携は「デモモードです」というアラートのみのスタブ)、ログアウトを提供。プロフィール編集の保存内容は `localStorage` に保存されます。

### 6.6 ナビゲーション(共通)

画面下部の4タブ(ホーム/探す/メッセージ/管理)。メッセージタブは未読件数、管理タブは報告待ち件数を赤バッジで表示。バッジは2秒間隔でポーリング更新されます。

---

## 7. 主要業務フロー

### 7.1 取引ライフサイクル(状態遷移)

```
applying(応募中) → offered(内定) → working(契約確定)
                                        ↓
                                 report_pending(完了報告待ち)
                                    ↙          ↘
                            disputed(異議あり)   completed(完了)
                                    ↓
                        (承認→completed / 却下→disputedのまま)

任意の段階から rejected(不採用・辞退) へ遷移可能
```

- `offered` → 応募者が承諾すると新しい `ContractTask`(`working`)が作られ、同一案件への他の競合応募は自動的に `rejected` になる
- `report_pending` → 完了報告時の評価が★2以下の場合、`report_pending` を経由せず即座に `disputed` へ遷移
- `disputed` の解消は「承認(→completed)」「却下(→disputedのまま、理由を再設定)」の2択

⚠️`rejected` は「クライアントに不採用にされた」場合と「応募者側が辞退した」場合の両方で同じステータス値が使われ、型定義上 `declined` という区別は存在しません。

### 7.2 プライバシー保護の仕組み

1. **位置情報のあいまい化**: 案件の正確な座標・店舗名は成約前は公開せず、エリア名・チェーン名レベルの抽象表示に変換([§6.2](#プライバシー保護ロジックsrcutilsmaskingutilsts))
2. **人材の匿名化**: `maskedName` のみを公開し、実名は成約後まで非公開
3. **連絡先マスキング**: チャット内のメール/電話番号/LINE IDを自動検知して非公開表示に置換([§6.3](#63-メッセージmessage-messagepagetsx))

これらはすべて「マッチング手数料を払わずに当事者間で直接契約されてしまう」ことを防ぐための、Connexyのビジネスモデル上コアとなる抑止機構です。

### 7.3 手数料

| 名称 | 料率 | 説明 |
|---|---|---|
| マッチング手数料 | 10% | 振込予定額から差し引かれる、Connexyの取次手数料 |
| 早期出金手数料 | 7.5% | 通常の振込サイクルを待たず即時振込を申請した場合に追加でかかる手数料 |

⚠️ダッシュボード上の金額表示は固定モック値であり、上記料率に基づく実計算ロジックは請求書PDF生成部分にも実装されていません(`price` フィールドをそのまま出力するのみ)。

---

## 8. 現状の実装状況に関する注記

本書内で ⚠️モック と記載した箇所に加え、実装全体を通じて以下の傾向があります。

- **地図・検索まわり以外はSupabase接続がほぼ使われておらず**、多くの画面が `src/data/mockDb.ts` のモックAPI経由でしか動作を確認できていません(接続断時は自動でオフラインモードへフォールバックする設計のため、実運用上も気づきにくい)。
- 「出勤ログ」「研修受講履歴」等、本来別テーブルで持つべきデータが `Staff.completedTrainings` という1つの文字列配列にタグ形式で混在しており、データモデルとして脆弱です。
- 案件の新規作成・編集フォームなど、UI上の導線はあるはずの機能で実装ファイルが見つからない箇所があります(本書 §6.4 参照)。
- ログイン画面のデバッグパネル([§4.3](#43-テストアカウント機能デバッグパネル))は本番リリース前に必ず対応が必要です。

既知のバグ・今後の対応タスクは [todo.md](todo.md) を参照してください。
